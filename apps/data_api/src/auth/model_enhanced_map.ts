import { Authorized } from "type-graphql";
import { ModelsEnhanceMap } from "@generated/type-graphql";
//import { Role } from "../interfaces";

export const modelsEnhanceMap: ModelsEnhanceMap = {
  // ShoppingCart: { fields: { _all: [Authorized(Role.USER)] } },
  // ShoppingCartItem: { fields: { _all: [Authorized(Role.USER)] } },
  // User: { fields: { _all: [Authorized(Role.USER)] } },
};
